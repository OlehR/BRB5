<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BRB6.Template.WareItemTemplate">
    <Grid ColumnSpacing="1" RowSpacing="1" Padding="1" 
       BackgroundColor="#adaea7">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Назва товару -->
        <Label x:Name="NameWaresLabel" Grid.ColumnSpan="5" Grid.Row="0" />

        <!-- Код товару -->
        <Label x:Name="CodeWaresLabel" Grid.Column="0" Grid.Row="1" />

        <!-- Кількість -->
        <Label x:Name="QuantityLabel" Grid.Column="1" Grid.Row="1" />

        <Label x:Name="ExpirationDateLabel" Grid.Column="2" Grid.Row="1" />
        <!-- Кількість введена -->
        <Label x:Name="QuantityInputLabel" Grid.Column="3" Grid.Row="1" />

        <Label x:Name="ExpirationDateInputLabel" Grid.Column="4" Grid.Row="1" />
    </Grid>
</ContentView>
